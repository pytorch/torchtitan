name: 8 GPU Integration Test on H100

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'torchtitan/experiments/**'
  pull_request:
    types: [labeled, synchronize]
    branches: [ main ]
    paths-ignore:
      - 'torchtitan/experiments/**'
  schedule:
    # Runs every 6 hours
    - cron: '0 */6 * * *'

concurrency:
  group: unit-test${{ github.workflow }}-${{ github.ref == 'refs/heads/main' && github.run_number || github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash -l -eo pipefail {0}

permissions:
  id-token: write
  contents: read

jobs:
  # Step 1: Dynamically compute the matrix based on conditions. This workflow
  # only runs the CUDA (H100) entry; the ROCm entry of the same test suite runs
  # in integration_test_8gpu_rocm.yaml on linux_job_v2.
  set-matrix:
    if: github.event_name != 'pull_request' || contains(github.event.pull_request.labels.*.name, 'ciflow/h100.8')
    uses: ./.github/workflows/set-matrix.yaml
    with:
      runner-cuda: mt-l-bx86iamx-176-1800-h100-8
      gpu-arch: cuda

  # Step 2: H100 CUDA runner uses linux_job_v3.
  build-test:
    needs: set-matrix
    if: needs.set-matrix.outputs.has-entries == 'true'
    uses: pytorch/test-infra/.github/workflows/linux_job_v3.yml@main
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.set-matrix.outputs.matrix) }}
    with:
      runner: ${{ matrix.runner }}
      gpu-arch-type: ${{ matrix.gpu-arch-type }}
      gpu-arch-version: ${{ matrix.gpu-arch-version }}
      # Private-ECR image, tagged with the .ci/docker hash from set-matrix.yaml.
      docker-image: 308535385114.dkr.ecr.us-east-1.amazonaws.com/torchtitan/${{ matrix.docker-image }}:${{ needs.set-matrix.outputs.docker-hash }}
      repository: pytorch/torchtitan
      upload-artifact: outputs
      timeout: 45
      script: |
        set -eux

        # The generic Linux job chooses to use base env, not the one setup by the image
        CONDA_ENV=$(conda env list --json | jq -r ".envs | .[-1]")
        conda activate "${CONDA_ENV}"

        # Log CUDA driver version for debugging.
        DRIVER_VERSION=$(nvidia-smi --query-gpu=driver_version --format=csv,noheader | head -n 1 || true)
        echo "CUDA driver version: ${DRIVER_VERSION}"

        pip config --user set global.progress_bar off

        TORCH_SPEC="torch"
        if [ -n "${{ matrix.torch-version }}" ]; then
          TORCH_SPEC="torch==${{ matrix.torch-version }}"
        fi

        # Pre-install torch's pure-python deps from the in-cluster pypi-cache for speed.
        python -m pip install filelock typing-extensions "setuptools<82" sympy networkx jinja2 fsspec numpy
        # Uninstall any pre-existing torch so the nightly below installs cleanly
        # without --force-reinstall (which would re-download torch's deps from
        # the public PyPI CDN instead of the in-cluster cache).
        python -m pip uninstall -y torch
        # Clear PIP_EXTRA_INDEX_URL so the default cpu index can't supply a +cpu torch.
        PIP_EXTRA_INDEX_URL= python -m pip install --pre "${TORCH_SPEC}" --index-url ${{ matrix.index-url }}

        USE_CPP=0 PIP_EXTRA_INDEX_URL= python -m pip install --pre torchao --index-url ${{ matrix.index-url }}

        mkdir -p "$RUNNER_TEMP/artifacts-to-be-uploaded"

        # Install DeepEP for HybridEP integration test
        bash /install_deepep.sh

        # Enable CPP stacktraces for debugging symmetric memory initialization errors.
        # Disable Nvlink Sharp. The CI machine seems to be unstable state to support
        # NLVS according to several CI runs.
        # DeepEP needs CUDA_HOME specified to JIT kernels.
        CUDA_HOME=/usr/local/cuda NCCL_NVLS_ENABLE=0 TORCH_SHOW_CPP_STACKTRACES=1 python -m tests.integration_tests.run_tests --test_suite h100 $RUNNER_TEMP/artifacts-to-be-uploaded --ngpu 8
        rm -rf $RUNNER_TEMP/artifacts-to-be-uploaded/*/checkpoint
