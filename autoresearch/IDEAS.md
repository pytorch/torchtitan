# Autoresearch Ideas

This file is intentionally empty for Run 1.

Run 1 is the **pure autonomy baseline**: the agent generates its own ideas
from FX graph inspection, without any human-curated seeds.

## Format

Each idea is a top-level bullet with a status checkbox:
- `[ ]` — open, not yet explored
- `[~]` — partially explored, more work possible
- `[x]` — fully explored or no further opportunity

Comments and findings go as **indented sub-bullets** with a timestamp at
the beginning:

```
- [ ] **Idea name**: Description.
  - YYYY-MM-DD HH:MM — Finding or comment.
```

## Ideas

- [x] **Identity slice + double-transpose elimination** (Exp 7 keep, +2.4%): extend Exp 1's peephole-cleanup story. `aten.slice.Tensor(x, dim, 0, end, 1)` is a no-op when `end >= size(dim)`; `aten.t(aten.t(x))` cancels to `x`; `aten._to_copy(x, dtype=x.dtype)` is identity. All numerics-safe.
  - 2026-05-22 — Exp 7 keep: 421/421 slices (100% — every slice was a full-range DTensor local-extraction), 450/1125 double-`t` (40%, mostly backward grad-accumulation `t(t(W))`), 0/841 identity casts. tps 4,569 (best of 2, +2.4% vs 4,460). Memory unchanged (slice/t are metadata-only; win is launch/dispatch overhead, not deallocation as in Exp 1). Pair with `remove_identity_views` as the standard cleanup prologue.

- [x] **View-chain collapse + alias / detach / expand elimination** (Exp 9 discard, +0.2% noise): conservative view-chain collapse (single-user inner) hit 1/1352, no alias/expand nodes in graph, 194/194 detach nodes eliminated. Numerics OK, memory unchanged. Metadata-cleanup well is largely tapped out. *Aggressive* view-chain collapse (clone inner views with N>1 users) is still untried — could expose more downstream fusion if a future pass benefits from a simpler graph topology, but standalone unlikely.

- [ ] **Fuse FSDP weight all-gather + dtype cast** (forward + backward weight re-gather): every parameter is materialized as `view -> view -> _to_copy(f32->bf16) -> all_gather_into_tensor(group=4 '42') -> wait_tensor -> slice -> view -> view` before the matmul that consumes it. Replace the per-param `_to_copy + all_gather` with a single fused all-gather that emits bf16 directly (or shifts the cast onto the smaller pre-AG shard), and drop the dead identity `view`/`slice` chain. Skips ~291 copies and many launches per step.
  - 2026-05-21 — From recon: 291 fwd+bwd FSDP all-gathers on mesh '42', each preceded by a `_to_copy` to bf16. AG shapes are roughly `[64128, 4096]`, `[7168, 4096]` x2, `[1024, 7168]`, `[2048, 4096]`, `[512, 4096]` x2, `[1024, 2048]`, plus 3 RMSNorm weight `[1024]` gathers per layer x32 layers.

- [x] **Rewrite TP `split([N,N],1) -> cat([·,·],0) -> reduce_scatter('sum', 2, '21')` as direct reduce_scatter on dim 1**: this pattern fires after every TP-sharded matmul (wo, w2, lm_head, and once after `tok_embeddings`). The split+cat is a "rearrange-then-RS-on-dim-0" trick that wastes a full activation-sized concat allocation. A custom `reduce_scatter_v` on the sequence dim, or a `reduce_scatter` on a non-contiguous strided view, would avoid the intermediate `[2, 4096, 4096]` tensor entirely.
  - 2026-05-21 — From recon: 130 `reduce_scatter_tensor(..., 2, '21')` calls, each immediately preceded by `split.Tensor(x, 4096, 1); getitem[0]; getitem[1]; cat([·, ·])`. Activation shape per layer is bf16 [1,8192,4096]; the cat allocates 64MB per occurrence.
  - 2026-05-22 — Exp 11 keep: 130/130 cats elided (zero skips of any kind). Math proof: when split-source x is contiguous, `cat([split[0], split[1]], dim=0)` has byte-identical layout to `x.view([2, N, D])`. Replaced cat with view; DCE removed split + getitems. tps **4,641** (both runs identical) = **+1.58% over 4,569 baseline**. mfu 27.17% (+0.41pp). Memory floor unchanged (47.05 GiB) — cats are short-lived intermediates that don't anchor the peak. **Win is reduced memcpy bandwidth + launch count.** NCCL accepted the view input without complaint. Numerics bitwise-identical (both runs to baseline). Cumulative improvement over raw baseline: +11.5% (4,161 → 4,641).

- [x] **Eliminate identity DTensor view chains**: every DTensor op materializes as `view_as(input)` + `view_as(local_tensor)` pairs that pass identical shapes/strides through. The graph has 715 `input.view_as(input)` and 1228 `local_tensor.view_as(local_tensor)` calls — ~1.9K nodes that compile/launch for no reason. A peephole pass that replaces such no-op `view.default(x, list(x.shape))` with the input should be very cheap to write and shrink the graph substantially.
  - 2026-05-21 — From recon: 3358 `aten.view.default` nodes total; the vast majority annotated with the DTensor `_api.py:112 / _api.py:229` source lines are zero-cost in eager but still emit FX nodes that take pass/compile time and can confuse downstream fusion.
  - 2026-05-21 — Exp 1 keep: pass removed 2006/3358 view nodes (more than estimated). tps 4,161→4,460 (+7.2%), peak memory 49.0→47.0 GiB (-2.0). Numerics bitwise identical. 1222 genuine reshapes preserved. Surprise: memory dropped — identity views were anchoring tensor references and delaying deallocation post-AOT.

- [x] **Overlap FSDP all-gather with prior layer's compute (prefetch)**: the forward executes `AG_layer_i -> wait -> matmul_i -> ...` strictly serially. Reordering / scheduling so that `AG_layer_{i+1}` issues before `matmul_i` completes (and `wait` is delayed until just before the matmul that needs it) would hide the all-gather latency behind compute. A pass that walks the graph, finds (`AG_w`, `wait_w`, ..., `mm`) chains, and moves the AG earlier (subject to dependency) is feasible at the FX level.
  - 2026-05-21 — From recon: 291 weight AGs all use group '42' (FSDP dp_shard, size 4). Each is followed by a `wait_tensor` and a single `mm`. Currently the AG and the mm sit on the same chain with no overlapping op between them, so the wait blocks compute.
  - 2026-05-21 — Exp 3 discard (Strategy A only): pure-reorder pass moved 130/421 AGs by avg 3.5 slots; tps 4,371 (slight regression vs 4,460). Each AG's *input* (`_to_copy` of the shard) is produced layer-locally, so "earliest legal position" is still inside the same layer — no cross-layer overlap won. To get real prefetch we'd need to move the AG's producer chain (cast+view+AG together) above the previous layer's wait — bigger surgery. Strategy B not attempted in this iteration.
  - 2026-05-21 — Exp 5 discard (Strategy B, first attempt): hoisted producer chains above the *immediately preceding AG's wait*. 290/421 AGs hoisted, avg 10.6 slots / 1.2 wait barriers crossed. tps 4,481 (best of 2; +0.5%, below threshold). Movement turned out to be *intra-layer*, not cross-layer, because the "preceding AG wait" is the same layer's previous weight wait. To get genuine cross-layer prefetch, anchor `prev_wait` to the last `wait_tensor` of the **previous layer's residual reduce_scatter** (the layer's true compute barrier), not just the previous AG. Memory unchanged. Numerics OK. Worth another try with the corrected anchor.
  - 2026-05-22 — Exp 6 discard (Strategy B v2, "most recent TP-RS wait" anchor): 290/291 weight AGs hoisted, avg 20.5 slots / **1.0 barriers** crossed each. tps 4,467 (+0.16%, well below threshold). Memory bump only +0.2 GiB (vs the larger bump expected if real prefetch were happening). Root cause: 130 RS-waits exist (forward + backward) — fwd has 2 per layer (after `wo` mid-layer, after `w2` end-of-layer), so "most recent RS-wait" usually lands at a mid-layer barrier, still intra-layer. To get true cross-layer prefetch, must filter to **post-`w2` (end-of-layer) RS-waits only**, OR anchor on the *previous-layer*'s last RS-wait. Also learned: NCCL group-name tags (`'42'`, `'21'`, `'19'`) are **unstable across runs** because they depend on PG creation order. Match by group **size** (FSDP=4, TP=2) instead.
  - 2026-05-22 — Exp 8 discard (Strategy B v3, end-of-layer-only RS-wait anchor): filtered the 130 RS-waits to 66 layer-end barriers (every-other in fwd/bwd node order), confirmed structurally by gap analysis (attention block ~210 lines, FFN block ~127 lines). 290/291 weight AGs hoisted, avg 47 slots / **1.44 barriers** crossed each (close to target ≥1.5). Memory +0.2 GiB. **tps 4,549 (-0.4% vs 4,569)** — actually regressed. Marking `[x]`: 4 prefetch attempts, all null-or-negative. Strong evidence that either (a) NCCL stream is already serialized at the runtime level so launching the AG earlier in node order doesn't create real overlap, or (b) AOT-trace runtime doesn't issue collectives onto a separate stream as expected. Either way, FX-level node reordering is not the right lever for this scale. Stop chasing prefetch via reordering.

- [~] **Bucket consecutive small-weight all-gathers (Q/K/V, w1/w3, RMSNorm γ)**: per layer we issue separate AGs for Q [2048,4096], K [512,4096], V [512,4096], wo [4096,2048], w1 [7168,4096], w2 [4096,7168], w3 [7168,4096], attention_norm γ [4096], ffn_norm γ [4096]. Q/K/V share the input (post attention_norm) and could be fused into a single `[3072,4096]` AG; w1/w3 likewise into `[14336,4096]`. The two RMSNorm γ AGs per layer (just 4096 bf16 each) are pure launch overhead and could be merged with a sibling AG.
  - 2026-05-21 — From recon: Q/K/V AGs land at lines 680/706/732 (etc.) back-to-back with identical group/mesh. w1/w3 likewise at 890/919. The K and V AGs are 8KB each — well below NCCL's bandwidth-bound threshold.
  - 2026-05-21 — Exp 4 discard (Q/K/V only): bucketed 32 triplets (64 AG launches saved/step). Numerics bitwise-identical. tps 4,490 (best of 2) vs 4,460 baseline = +0.7%, below the +1% keep threshold. Conclusion: Q/K/V AGs at 4-rank NVLink are *bandwidth-bound*, not launch-bound — the Q shard alone is 64KB and dominates. cat+split/reshape overhead in the rewrite cancels most of the launch win. Likely-better targets: the truly tiny RMSNorm γ AGs (4KB at FSDP=4) or full per-layer bucketing using a coalesced AG op if one exists.

- [ ] **Move bf16 cast from full weight to shard before AG**: `_to_copy(f32 -> bf16)` runs on the **pre-AG sharded** weight (e.g. `[1792,4096]`), which is correct shape-wise but still ~4x more data than needed if the weight already lived in bf16. If the FSDP parameter could be kept in bf16 on the shard and AG'd directly, the `_to_copy.default` (420 occurrences, all fp32->bf16) disappears and AG bandwidth halves on average. Alternatively, fuse the cast into the AG buffer prep.
  - 2026-05-21 — From recon: 842 `_to_copy.default` ops, almost evenly split between fp32->bf16 (FSDP weight cast, 420) and bf16->fp32 (RoPE/loss, ~420). Removing the FSDP-cast half is 420 fewer kernel launches.

- [ ] **Fuse RoPE complex-mul block into a single bf16 RoPE kernel**: per layer the graph emits `_to_copy(bf16->f32) -> view -> view_as_complex -> mul (complex) -> view_as_real -> view -> _to_copy(f32->bf16)` for both Q and K. That's ~12 ops per layer x32 = 384 nodes for what should be a single elementwise fused kernel. A pass that rewrites this subgraph into a single `aten.fused_rope` (or a custom HOP) reduces launches and avoids materializing the fp32 intermediate.
  - 2026-05-21 — From recon: 128 `view_as_complex` + 128 `view_as_real` + 64 `_conj` (backward) + ~256 surrounding `_to_copy` ops. Each layer has exactly 4 view_as_complex (Q, K) and 4 view_as_real, plus the two casts.

- [x] **Drop unused SDPA outputs (`getitem_2..5`)**: `_scaled_dot_product_flash_attention.default` returns 9 outputs; only `[0]` (out), `[1]` (logsumexp), `[6]` (philox seed), `[7]` (philox offset) are used. `getitem_6/7/8/9` are bound and then immediately set to None. Trim them at the FX level so the kernel/return tuple can be specialized; also let the partitioner skip materializing them in backward.
  - 2026-05-21 — From recon: 32 SDPA forward calls (one per layer). Every one shows `getitem_6 = ...; getitem_6 = None` pattern through `getitem_9`. Detach of `getitem_4` produces an unused `detach_2` on the forward output that is only used for backward graph wiring.
  - 2026-05-21 — Exp 2 discard: pass removed 0 nodes. The dead getitems shown in `/tmp/autoresearch_graph.txt` (pre-passes) are already DCE'd by `eliminate_dead_code()` inside `remove_identity_views`. tps 4,432 (run-to-run noise vs 4,460). No further upside here without changing the SDPA op's output schema (out of scope — only passes.py is modifiable).

- [ ] **Hoist DTensor `_to_copy` of constants out of the per-step graph**: RMSNorm weight casts (`f32->bf16`, shape `[1024]`) and parameter view chains execute every step despite operating on parameters whose stored dtype is static. If the FSDP MixedPrecisionPolicy cast is amortized into the optimizer step (or skipped when `param_dtype == storage_dtype`), the forward graph drops one cast + 2 views per RMSNorm γ and per Linear weight.
  - 2026-05-21 — From recon: 65 RMSNorm `_to_copy` casts on `[1024]` tensors (32 attention_norm + 32 ffn_norm + 1 final norm). Each cast is followed by an AG on `[4096]` — both are launch-bound, not bandwidth-bound.

- [ ] **Async grad reduce-scatter overlap with backward compute**: backward emits 130 RS-sum-mesh-21 (activation grads) and 161 RS-sum-mesh-42 (parameter grads). Each is `op -> reduce_scatter -> wait` strictly serial with the next matmul. Reorder the wait so the next matmul issues before this RS finishes; or batch multiple parameter-grad RSes into a single bucketed call (FSDP-style "grad bucketing" at graph level).
  - 2026-05-21 — From recon: 161 `reduce_scatter_tensor(..., 'sum', 4, '42')` (parameter grads, one per FSDP-sharded param per step) and 130 `reduce_scatter_tensor(..., 'sum', 2, '21')` (TP-sharded activation grads). Both currently followed immediately by `wait_tensor` in graph order.

### Re-recon 2026-05-22

Post-Exp1+Exp7 graph dumped to `/tmp/post_cleanup_graph.txt` (13,936 lines, down from 21,152 raw). The pure-metadata peephole well is tapped; the remaining hot patterns are mostly multi-user CSE, RoPE/cast structure, and the TP split→cat→RS rearrange. Op histogram (top 10): view 1352, wait_tensor 910, _to_copy 841, mm 675, t 450, AG 421, RS 421, transpose 256, mul/add 225, _unsafe_view 225.

- [x] **CSE duplicate `view([8192, 4096])` + `_conj` + `clone` (Exp 10 discard, +0.4%)**: 96 view + 32 _conj + 32 clone duplicates merged (exactly matching prediction). Numerics OK, memory unchanged. Below 1% noise threshold. Confirms: ≤200 ops/step of metadata cleanup is sub-noise. Future wins must target high-volume traffic (RoPE casts, TP cats, param-grad RSes), not more small-count CSE.
  - 2026-05-22 — From re-recon: 225 `view([8192, 4096])` nodes, 1352 views total post-cleanup.
  - 2026-05-22 — From re-recon: `_conj_1`/`_conj_2` both target `view_1871`; clones bit-identical.
  - 2026-05-22 — Exp 10 discard: 96 view + 32 _conj + 32 clone CSE'd, +0.4% best-of-2 (4,587/4,572 vs 4,569). Memory floor unchanged. The redundant clones don't anchor the peak — they live and die within a single backward-RoPE step.

- [ ] **Eliminate detach of `_fused_rms_norm[1]` (mean) and `_fused_rms_norm[0]` outputs**: 194 `aten.detach.default` nodes survive into the post-cleanup graph, all hanging off either `_fused_rms_norm[1]` (the saved-mean output used for backward, fp32 [1,4096,1]) or SDPA's `getitem_4` (saved attn matrix). `detach` is a metadata-only op that just blocks autograd retracing; in the AOT graph it has no semantic value, but it adds 194 dispatched no-op ops/step. Exp 9 confirmed they elide cleanly via `replace_all_uses_with(input)`. Exp 9 was discarded for being below noise floor as a *standalone* pass — but bundled with a higher-leverage pattern (CSE views, RoPE fusion) the cost is free and the unified pass might cross +1%.
  - 2026-05-22 — From re-recon: 194/194 detach nodes survive. Pattern verified: `getitem = _fused_rms_norm[1]; detach_K = detach(getitem)` and `getitem = SDPA[4]; detach_K = detach(getitem)`. Same structural pattern as Exp 9, but worth re-bundling with bigger wins.

- [ ] **Fuse `view → _to_copy(f32) → view → view_as_complex → mul → view_as_real → view → _to_copy(bf16)` RoPE block into a custom kernel or at least a tighter sub-pattern**: per layer, the RoPE block emits ~12 ops per Q+K = 384 launches/fwd, plus 64 `_conj` + 64 `clone` + 64 `mul.Tensor` + 64 `view_as_complex` on the backward = ~700 RoPE-related ops/step. The fp32 round-trip (`_to_copy.bf16→f32` then `_to_copy.f32→bf16`) materializes 64 fp32 buffers (`[1,8192,16,128]` = 16 MiB each + `[1,8192,4,128]` = 4 MiB each) per step = ~1.2 GiB of avoidable activation traffic on top of the launches. An FX rewrite that replaces the subgraph with `torch.ops.aten.linalg_vector_norm`-style fused complex-mul, or a custom HOP, is non-trivial but high-leverage. Lower-risk first step: drop the f32 round-trip by computing the complex mul in bf16 (numerics validation required).
  - 2026-05-22 — From re-recon: 128 view_as_complex + 128 view_as_real + 256 surrounding `_to_copy`s (half of all 841 `_to_copy`s in the graph are RoPE bf16↔f32 round-trips, the other half being FSDP weight casts). The fp32 round-trip is by far the heaviest remaining cast traffic.

- [ ] **Rewrite TP `split(x, N, 1) → cat([s0, s1], 0) → reduce_scatter(dim=0, ws=2)` as a strided reduce_scatter / view-only rearrange**: confirmed surviving pattern, 130 occurrences (= 1 fwd + 1 fwd-residual + bwd × 32 layers + ends). Each `cat` allocates **`bf16[2, 4096, 4096]` = 64 MiB** of intermediate buffer — total **~8.3 GiB of cat allocations/step** that could be elided. The split+cat is a "swap seq-half ↔ batch-axis" trick to convert sequence-parallel scatter into a dim-0 RS. If we issue `reduce_scatter` on a strided non-contig view (`.view([2, 4096, 4096])` of the original `[1, 8192, 4096]` with explicit stride), the data is already in the right layout — only the metadata changes. Caveat: NCCL requires contiguous input, so the win depends on whether a contiguous `as_strided` already exists or a `clone` is forced. If `clone` is forced this won't pay; but the 64-MiB-per-RS budget makes even a partial win attractive.
  - 2026-05-22 — From re-recon: 130 split+cat pairs feed RS-ws-2; each cat output is `bf16[2,4096,4096]` stride `[16777216,4096,1]` (contiguous), each split input is `bf16[1,8192,4096]` stride `[33554432,4096,1]` (contiguous). The split halves have stride `[33554432,4096,1]` (the first half is contiguous, the second is offset but contiguous in the inner dim). Math: a `view([2,4096,4096])` of the original [1,8192,4096] (when the seq dim is contiguous) has the same byte layout as `cat(split(...))`. The `cat` may indeed be elidable.

- [ ] **Move FSDP weight `_to_copy(f32→bf16)` *after* the all-gather, halving AG bandwidth**: every FSDP weight is currently `_to_copy.f32→bf16` THEN AG: 420 fp32→bf16 casts on the *sharded* weight (e.g. `f32[1792,4096]` → AG to `bf16[7168,4096]`). If we instead AG the f32 shard (`f32[1792,4096]`) and cast post-AG to bf16, AG bandwidth is **2× higher**, but the AG buffer is also 2× larger. Net win only if AG is launch-bound at the shard-byte regime (it isn't, per Exp 4). **Inverse direction is the win:** cast f32→bf16 BEFORE sharding (i.e. keep param in bf16 storage), but the only `passes.py`-scope way is to *de-duplicate* per-layer recasts — currently each parameter is cast on every step despite being static. If the same `arg_K` is `_to_copy`d only once and the result reused across fwd+bwd, we save 1 cast per param. Out-of-scope for this experiment (cast result is naturally per-callsite). Marking as low priority.
  - 2026-05-22 — From re-recon: 420 `_to_copy → bf16` casts target FSDP weight shards (4 unique shard shapes: `[1792,4096]`, `[1024,7168]`, `[512,4096]`, `[1024,2048]`, plus norm γ `[1024]`). Each cast happens once per param per step; the *graph* shows them as unique nodes per call-site, so plain CSE within the graph wouldn't merge fwd-vs-bwd recasts (they don't co-exist). Pivot the framing: see also the dtype-policy idea above (already `[ ]`).

- [ ] **Bucket parameter-grad `reduce_scatter(ws=4)` calls into per-layer coalesced RS**: backward emits 161 RS-ws-4 calls (one per FSDP-sharded param per step), each followed by a `wait_tensor` — so 161 launches dedicated to param grads alone. Each layer has ~9 weights (q,k,v,wo,w1,w2,w3,attn_norm,ffn_norm) → 9 RS-ws-4 per layer. Bucketing all 9 into a single concatenated RS-ws-4 saves 8 launches/layer × 32 = 256 launches/step. Caveat from Exp 4: bucketing collectives that are bandwidth-bound (Q shard is 64 KB at FSDP=4) gives no win; here the param grads have a different size distribution — RMSNorm γ grads are tiny (4 KB shard) and IDEAL bucketing candidates. Selective bucketing of only the small param-grad RS is the safer angle.
  - 2026-05-22 — From re-recon: 161 RS-ws-4 calls fire in the backward — 5× the count of fwd FSDP AGs (291 fwd+bwd AGs, of which 130 fwd). Pattern: each `mm` backward emits its weight-grad RS immediately after the `t_X` and matmul. Grouping the bwd RSes is structurally similar to Exp 4's Q/K/V bucket but on the *RS* side, which Exp 4 didn't try. Different launch regime: param grads are ALL relatively small (per-layer total ≈ 100 MB sharded → 25 MB/rank).

- [ ] **Collapse 3-way duplicate `t.default(wait_tensor_K)`** if any: 450 `t.default` remain (Exp 7 cancelled 450/1125 cancelling pairs). The remaining `t.default(wait_tensor_K)` calls each transpose a 2D weight just before its `mm`. If any wait_tensor feeds multiple `t.default`s with the same shape, CSE them. **Low priority**: spot check shows each `t_K = t.default(wait_tensor_K)` has a unique wait_tensor source (each AG result feeds at most one mm), so this is likely a no-op pattern in the current graph. Flag and verify.
  - 2026-05-22 — From re-recon: sample `t.default(wait_tensor_4..wait_tensor_57)` shows 1:1 mapping with no immediate duplicate. Verify via uses-count before pursuing.

### Re-recon 2026-05-22 post-Exp11

Post-Exp1+Exp7+Exp11 graph dumped to `/tmp/post_exp11_graph.txt` (13,546 lines, down from 13,936 pre-Exp11). Op histogram delta vs `/tmp/post_cleanup_graph.txt`: `cat.default` 130 → **0** (gone), `view.default` 1352 → **1482** (130 new views replacing the cats). Everything else **unchanged**: `_to_copy` 841, `mm` 675, `t` 450, AG 421, RS 421, `wait_tensor` 910, `_unsafe_view` 225, `detach` 194, `view_as_complex/real` 128 each, `_conj` 64, SDPA 32. The 130 new views are `view([2, 4096, 4096])` of `bf16[1, 8192, 4096]` immediately feeding RS-ws-2; **already zero-copy**, no follow-on peephole available there.

The TP `AG→view([1, 8192, 4096])` pattern in fwd (the "natural inverse" of Exp 11) is **already optimal** — 130/130 TP AGs are followed by a single zero-copy view (no AG→split pattern exists; the split-along-seq is folded into the RS layout on the producing side, and into the view on the consuming side). No Exp-11-style win on the bwd side.

The TWO biggest remaining sources of per-step traffic, by far:

1. **Param-grad bf16→f32 upcast + RS-ws-4 in f32** — 291 occurrences, all f32 RS inputs totaling **~9.5 GiB of f32 RS bandwidth + 9.5 GiB f32 cast write/step**. The cast is mandated by FSDP's MixedPrecisionPolicy (reduce_dtype=f32 over bf16 params) and is necessary for numerics. But the surrounding *structure* is exploitable.
2. **RoPE fp32 round-trip** — `bf16 → _to_copy → f32 → view → view_as_complex → mul → view_as_real → view → _to_copy → bf16` for Q+K, fwd+bwd, every layer. **~7.5 GiB/step** of fp32 activation alloc+copy traffic.

- [~] **Bucket param-grad RS-ws-4 by FSDP group at layer boundary**: 291 RS-ws-4 calls with 291 immediately-following `wait_tensor`s = 582 collective-related launches/step. Each layer has ~9 weights (q/k/v/wo/w1/w2/w3 + 2 RMSNorm) → ~9 RS-ws-4 per layer in bwd. Coalescing all 9 per layer into a single concatenated RS would save 8/9 launches × 32 layers + endpoints ≈ 256 launches/step. Unlike Exp 4 (Q/K/V AG, bandwidth-bound at 64 KB), the RMSNorm γ-grad shards are **1 KiB each** (`f32[1024]`/`f32[4096]`) — pure launch overhead. Per-layer total grad-tensor-bytes ≈ 90 MiB (f32), well above NCCL's launch-bound threshold, so the *coalesce* should dominate any cat overhead. **Highest-leverage remaining target by both cumulative traffic and launch count.**
  - 2026-05-22 — From re-recon: 96 `f32[512,4096]` (Q/K/V grads, 8 KiB each) + 65 each `f32[4096]`/`f32[1024]` (norm grads, 4 KiB) + 64 `f32[7168,4096]`/`f32[1792,4096]`/`f32[128,4096]` + 32 each `f32[4096,7168]`/`f32[4096,2048]`/`f32[2048,4096]`/`f32[1024,7168]`/`f32[1024,2048]` + 2 lm_head. Cast-then-RS-then-wait at each callsite. The fwd-side analog (mid-layer RS-ws-2) is on a different mesh, so bucketing has to be per-axis.
  - 2026-05-22 — Exp 12 discard (backward RS only): **`reduce_scatter_tensor_coalesced.default` exists** as a functional collective op — used directly, no cat/split fallback needed. 33 buckets, avg size 8.82, 291/291 RSes coalesced (100% coverage, zero structural skips). Numerics bitwise-identical. tps 4,678 (best of 2 = +0.80% vs 4,641 baseline), just below +1% keep threshold. **Coalesced collective still bandwidth-bound on NVLink at FSDP=4** — NCCL apparently issues per-tensor reductions internally so FX-level launch savings don't directly translate. Forward AG coalescing (same mechanism) likely faces the same ceiling. Strategy: bundle forward AG + backward RS coalescing in one pass to compound.

- [ ] **Drop the RoPE fp32 round-trip (compute RoPE in bf16)**: per-layer Q+K = 4 × (`_to_copy bf16→f32` then `view_as_complex` then `mul` then `view_as_real` then `_to_copy f32→bf16`) = 12-16 ops × 64 layer-sides = ~700 RoPE ops/step. Fp32 intermediates alone are **~7.5 GiB/step** of activation alloc+copy. Lower-risk first step: drop the two casts and compute the complex mul directly on the bf16 input by viewing bf16 as complex32 (`c32`) — torch supports `view_as_complex` for fp32 only, so the cast cannot trivially be removed without a kernel-level change. **Higher-leverage middle ground**: rewrite the block as a single fused custom HOP that takes bf16 Q/K and the f32 RoPE freqs, does the complex mul internally, and emits bf16 — keeps numerics close but elides the 7.5 GiB. Non-trivial to implement; estimate one to two iterations. Numerics validation will require `--debug.deterministic` loss/grad-norm comparison.
  - 2026-05-22 — From re-recon: half of all 841 `_to_copy`s (≈420) are RoPE bf16↔f32 round-trips. Removing them removes a third of `_to_copy` traffic and the matching f32-buffer alloc traffic, AND simplifies the bwd RoPE block (`_conj`/`clone`/`mul`/`view_as_real` chain).

- [ ] **Fuse the FSDP weight cast + AG into a single bf16-source-shard AG (or pre-cast to bf16 in optimizer)**: 420 `_to_copy(f32→bf16)` on FSDP weight shards = **~5.0 GiB/step** of cast traffic (f32 read 3.3 GiB + bf16 write 1.7 GiB). Two angles: (a) keep FSDP shards in bf16 storage so the per-step cast vanishes (out of `passes.py` scope but mention for completeness); (b) within `passes.py` scope, fold the cast and the AG into a single `coalesced_all_gather_to_bf16` HOP that reads f32, casts, and gathers in one go — halves activation alloc traffic, single launch. Less invasive than rewriting RoPE.
  - 2026-05-22 — From re-recon: 420 f32→bf16 casts on shard shapes `[1792,4096]`, `[1024,7168]`, `[512,4096]`, `[1024,2048]`, `[128,4096]`, and 65 norm γ `[1024]`. Each cast is in the AG producer chain — eliminating it would also reduce the bytes Exp 3/5/6/8 were trying to hoist for prefetch.

- [ ] **Eliminate the bf16→f32 cast on TP all_reduce inputs for cross-entropy** (`amax`, `sum_1`, `index_put__3`): the loss path emits 3 `all_reduce(... 'sum'/'max', '21')` calls in f32, each preceded by a small cast. Total bytes are small (`f32[8192, 1]` = 32 KiB each), so this is **launch-overhead only** (6 launches incl. wait). Low leverage by itself, but combined with the param-grad bucketing pass it costs nothing extra. **Defer to a bundled cleanup pass**, do not pursue standalone.
  - 2026-05-22 — From re-recon: 68 total all_reduce calls; 3 in the loss reduction, 65 unaccounted-for (one per parameter? Verify — may be the `all_reduce` from grad norm computation or DDP-style residual). Inspect before bundling.

- [ ] **Coalesce the 130 TP-RS-ws-2 calls per-layer-pair (post-`wo` + post-`w2`)**: each layer emits 2 RS-ws-2 on `bf16[2,4096,4096]` (now view → RS thanks to Exp 11). If we let both fire as one combined RS on `bf16[4,4096,4096]`, we halve the layer's TP-RS launches (32 × 2 → 32 × 1 = -32 launches/step + endpoints). The catch: post-`wo` RS feeds rmsnorm immediately, post-`w2` RS feeds the next layer's residual-add — coalescing means buffering the post-`wo` result for an entire FFN block, delaying its consumer. **Likely a regression**, but worth verifying that the dependency chain forces serial RS issue today (which it does in the FX node order). Flag low priority.
  - 2026-05-22 — From re-recon: pattern verified — `wait_tensor_1` (post-`wo`) feeds `_to_copy_1 → AG → ... → rmsnorm`, then `wait_tensor_8` (post-`w2`) feeds `add(wait_tensor_1, wait_tensor_8)`. The two waits are intra-layer dependent, so coalescing changes critical path. Probably not a win.

- [ ] **Hoist `detach.default` of `_fused_rms_norm[1]` saved-mean and SDPA `getitem_4`** (re-flag from prior recon, still 194/194 standing): Exp 9 confirmed they elide cleanly via `replace_all_uses_with(input)` but was discarded standalone at +0.2%. **Re-bundling rationale**: when packaged with the param-grad bucketing pass (which itself touches the RS+wait chain), the detach removal is a free 0-line addition. Don't run as a separate iteration.
  - 2026-05-22 — From re-recon: 194 detaches still present, all from `_fused_rms_norm[1]` and SDPA `getitem_4`. Pattern unchanged.

**Invalidated by Exp 11**:
- The "TP `split→cat→RS`" idea above (already `[x]`).
- Any future idea targeting `aten.cat.default` as a high-volume target: the only 130 occurrences are eliminated. Remaining cat use cases are not in this graph at all (post-Exp11 cat count is 0).
